{
  "name": "fetch-router-test",
  "type": "module",
  "dependencies": {
    "@remix-run/fetch-router": "^0.8.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.9.0"
  },
  "scripts": {
    "build": "tsc",
    "check": "tsc --noEmit",
    "test": "node --test src/index.ts"
  }
}
